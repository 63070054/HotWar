version: '3'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: my-frontend_image:1.0
    ports:
      - "3000:3000"
    restart: always
    command: ["yarn", "dev"]